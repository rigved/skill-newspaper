#!/usr/bin/expect -f

set password [lindex $argv 0]
spawn ./update_superuser_password.sh
expect -exact "Password: "
send -- "$password\r"
expect -exact "Password (again): "
send -- "$password\r"
set password ""
expect eof
